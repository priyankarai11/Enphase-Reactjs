import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  listStyle: {
    marginLeft: 70,
  },

  linkRoot: {
    color: "#f37320",
  },

  listContents: {
    marginBottom: 50,
  },

  checkbox: {
    marginLeft: 30,
    marginTop: 30,
    marginBottom: 10,
  },

  listItems: {
    marginBottom: 16,
    fontSize: 14,
    fontWeight: "bold",
    fontFamily: "Roboto",
  },

  formControlLabel: {
    fontSize: 16,
    fontWeight: "bold",
    fontFamily: "Roboto",
  },
});

export { useStyles };
