import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  roundIcon: {
    borderRadius: "10px",
    width: "15px",
    height: "15px",
  },
  labelStyle: {
    width: 160,
  },
});

export { useStyles };
